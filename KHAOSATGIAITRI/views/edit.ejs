<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh Sửa Khảo Sát</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        form {
            max-width: 600px;
            margin: 0 auto;
            background: navajowhite;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        input, select, textarea {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }
        button {
            padding: 10px 15px;
            background: #5cb85c;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background: #4cae4c;
        }
    </style>
</head>
<body>
    <h1>CHỈNH SỬA KHẢO SÁT</h1>
    <form action="/update/<%= entry._id %>" method="POST">
        <div class="form-group">
            <label for="name">Tên</label>
            <input type="text" id="name" name="name" value="<%= entry.name %>">
        </div>
        <div class="form-group">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="<%= entry.email %>">
        </div>
        <div class="form-group">
            <label for="age">Tuổi</label>
            <input type="number" id="age" name="age" value="<%= entry.age %>">
        </div>
        <!-- Add similar form groups for all other fields -->
        <div class="form-group">
            <label for="occupation">Nghề Nghiệp</label>
            <input type="text" id="occupation" name="occupation" value="<%= entry.occupation %>">
        </div>
        <div class="form-group">
            <label for="rating">Đánh Giá</label>
            <input type="text" id="rating" name="rating" value="<%= entry.rating %>">
        </div>
        <div class="form-group">
            <label for="favorite">Yêu Thích</label>
            <input type="text" id="favorite" name="favorite" value="<%= entry.favorite.join(', ') %>">
        </div>
        <div class="form-group">
            <label for="frequency">Tần Suất</label>
            <input type="text" id="frequency" name="frequency" value="<%= entry.frequency %>">
        </div>
        <div class="form-group">
            <label for="device">Thiết Bị</label>
            <input type="text" id="device" name="device" value="<%= entry.device %>">
        </div>
        <div class="form-group">
            <label for="platform">Nền Tảng</label>
            <input type="text" id="platform" name="platform" value="<%= entry.platform %>">
        </div>
        <!--  -->
        <button type="submit">Cập Nhật</button>
    </form>
</body>
</html>
